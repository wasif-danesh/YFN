# Future FastAPI settings, resolved from the repository root.
DATABASE_PATH=data/greater-melbourne-v1/yfn.sqlite
# JSON array; implement parsing in the backend settings model.
CORS_ALLOWED_ORIGINS=["http://localhost:3000"]
