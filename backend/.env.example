# FastAPI settings. Relative paths resolve from the repository root.
DATABASE_PATH=data/greater-melbourne-v1/yfn.sqlite
# JSON array of frontend origins; no path or trailing slash.
CORS_ALLOWED_ORIGINS=["http://127.0.0.1:3000"]
